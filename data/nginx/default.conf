upstream django {

    # * Service named in docker-compose.yml
    server web:8000;
}

server {
    listen 80;

    location / {

        # * Name defined in first line of this file
        proxy_pass http://django;
    }
}